// src/app/dashboard/settings/page.tsx
"use client";

export default function SettingsPage() {
  return (
    <div className="flex items-center justify-center min-h-[60vh] text-slate-400">
      <p>Settings page — select a category from the sidebar.</p>
    </div>
  );
}